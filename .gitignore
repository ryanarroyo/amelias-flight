node_modules/
dist/
*.tsbuildinfo

# tsc -b emit from the composite vite.config project
vite.config.js
vite.config.d.ts

# Design handoff scratch (original bundle from claude.ai/design)
.design-src/

# Local env / secrets (keep .env.example tracked)
.env
.env.local
.env*.local
.vercel
